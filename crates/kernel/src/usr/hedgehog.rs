use crate::api::process::ExitCode;

pub fn main(_args: &[&str]) -> Result<(), ExitCode> {
    println!(
        r#"
             _\/\|/_
hi im flin   c'///\/
               " "   
"#
    );
    Ok(())
}
